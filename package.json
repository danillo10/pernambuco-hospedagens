{
  "name": "pernambuco-maravilha",
  "private": true,
  "scripts": {
    "dev": "npm run dev:db && concurrently -n api,web -c blue,green \"npm run dev:api\" \"npm run dev:web\"",
    "dev:db": "docker compose up -d postgres",
    "dev:db:down": "docker compose down",
    "dev:api": "cd backend && ./venv/bin/uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev:web": "cd frontend && npm start",
    "build:web": "cd frontend && npm run build",
    "install:all": "cd frontend && npm install && cd ../backend && python3 -m venv venv && ./venv/bin/pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
